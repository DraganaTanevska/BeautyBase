@model BeautyBaseFinal.Models.Unavailable

@{
    ViewBag.Title = "Details";
}

<h2 style="color: lightcoral">Детали</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Date)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Date.ToString().Split(' ')[0])
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.selectedTime)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.selectedTime)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.UserName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ActionTime)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ActionTime)
        </dd>

        

    </dl>
</div>
<p>
    @{
        if (User.IsInRole("Admin"))
        {

            @Html.ActionLink("Зафатени термини", "Index")
            <br>
            @Html.ActionLink("Мои термини","UserAppointments")
        }
        else
        {

            @Html.ActionLink("Назад", "UserAppointments")
        }
    }
</p>
